// filter non-refinement values and return remaining values in a new object
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const filterRefinements = ({ page, query, sortBy, ...refinements } = {}) => refinements;

export default filterRefinements;
